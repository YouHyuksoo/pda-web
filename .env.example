/**
 * @file .env.example
 * @description
 * 환경변수 설정 예시 파일입니다.
 * 실제 사용 시 .env.local 파일로 복사하여 값을 설정하세요.
 *
 * 초보자 가이드:
 * 1. 이 파일을 .env.local로 복사
 * 2. 각 변수에 실제 값 입력
 * 3. .env.local은 git에 커밋되지 않음 (보안)
 */

# ===== Oracle Database =====
ORACLE_USER=hsgmes
ORACLE_PASSWORD=hsgmes
ORACLE_HOST=10.1.45.4
ORACLE_PORT=1521
ORACLE_SID=HSMESDB

# ===== NextAuth (인증) =====
# 랜덤 문자열 생성: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# ===== App 설정 =====
NODE_ENV=development

# ===== 서버 URL (API 호출용) =====
NEXT_PUBLIC_API_URL=http://localhost:3000/api
